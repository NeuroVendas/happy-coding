{
  "name": "happy-coding-desktop",
  "version": "0.5.0",
  "private": true,
  "description": "Happy Coding =] desktop browser prototype",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check chrome.js && node --check browser-core.js && node --check download-core.js && node --check library-core.js",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "electron": "44.2.0"
  }
}